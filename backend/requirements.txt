# All backend dependencies.
#
# Pinned to Django 6.0.x: DRF 3.17.2 (latest) imports
# django.utils.cache.cc_delim_re, which Django 6.1 removed. Revisit when DRF
# ships 6.1 support.

Django==6.0.8
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.1

psycopg2-binary==2.9.12
pgvector==0.4.1

python-decouple==3.8
pillow==12.3.0
drf-spectacular==0.28.0

# Production serving
gunicorn==23.0.0
whitenoise==6.9.0
Faker==40.36.0
# AI. google-genai is the only client library the agent uses — see
# ai/client.py, which is the one place the provider is named.
google-genai==2.20.0

# Text extraction for the RAG ingest pipeline.
pypdf==6.16.2
