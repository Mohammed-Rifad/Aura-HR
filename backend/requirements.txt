# Core backend dependencies.
# AI/RAG stack lives in requirements-ai.txt (installed on Day 12) — it pulls in
# torch, which is a ~2GB download we don't need until the agent work starts.

# Pinned to 6.0.x: DRF 3.17.2 (latest) imports django.utils.cache.cc_delim_re,
# which Django 6.1 removed. Revisit when DRF ships 6.1 support.
Django==6.0.8
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.1

psycopg2-binary==2.9.12
pgvector==0.4.1

python-decouple==3.8
pillow==12.3.0
drf-spectacular==0.28.0

# Production serving
gunicorn==23.0.0
whitenoise==6.9.0
