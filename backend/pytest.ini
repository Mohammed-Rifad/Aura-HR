[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py

# --reuse-db keeps the test database between runs. Creating it fresh every
# time means waiting on Neon for a minute before a single test runs.
addopts = --reuse-db
