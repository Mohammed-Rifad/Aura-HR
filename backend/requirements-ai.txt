# AI / RAG stack — install on Day 12.
#   pip install -r requirements-ai.txt
#
# sentence-transformers pulls in torch (~2GB). Embeddings run locally so no
# employee document content is sent to a third party for vectorisation.

-r requirements.txt

langgraph
langgraph-checkpoint-postgres
langchain-anthropic
langchain-core

sentence-transformers

# Document text extraction for the RAG pipeline
pypdf
python-docx
